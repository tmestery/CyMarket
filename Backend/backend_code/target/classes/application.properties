server.port = 8080

#spring.h2.console.enabled=true
#spring.datasource.url=jdbc:h2:mem:testdb

spring.datasource.url=jdbc:mysql://coms-3090-056.class.las.iastate.edu:3306/coms309?useSSL=false
spring.datasource.username=dmvp01
spring.datasource.password=Coms309
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.show_sql=true

# Mail configuration
# Set these properties (or the SPRING_MAIL_* environment variables) to match the SMTP provider
# that will send recovery emails. For production you typically need the provider host, port,
# an authenticated username/password, and to enable STARTTLS if required (e.g. Gmail, SendGrid,
# or your institution's SMTP relay).
spring.mail.host=${SPRING_MAIL_HOST:localhost}
spring.mail.port=${SPRING_MAIL_PORT:1025}
spring.mail.username=${SPRING_MAIL_USERNAME:}
spring.mail.password=${SPRING_MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_SMTP_AUTH:false}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_SMTP_STARTTLS:false}

