<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.LinearLayoutCompat
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".BuyActivity">

    <!-- temp header -->
    <TextView
        android:id="@+id/buy_msg_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Make a Purchase"
        android:textStyle="bold"
        android:layout_gravity="center"
        android:layout_marginTop="50dp"
        android:layout_marginBottom="50dp"
        android:textSize="36dp"
        android:textColor="#fc0303">
    </TextView>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/buy_listings_recycler"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        tools:listitem="@layout/item_listing_card" >
    </androidx.recyclerview.widget.RecyclerView>

    <Space
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">
    </Space>

    <!-- clear all button, for admins/temp-->
    <Button
        android:id="@+id/buy_clear_all_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Clear All"
        android:layout_marginTop="16dp"
        android:layout_gravity="center_horizontal">
    </Button>

    <!-- back btn -->
    <Button
        android:id="@+id/buy_back_btn"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Back"
        android:textSize="20sp"
        android:textColor="#ffffff">
    </Button>

</androidx.appcompat.widget.LinearLayoutCompat>